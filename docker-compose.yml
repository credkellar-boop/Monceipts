services:
  api:
    build: .
    depends_on:
      - db
      - redis
    environment:
      - REDIS_URL=redis://redis:6379
      
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  db:
    image: postgres:15-alpine
    # ... previous config
